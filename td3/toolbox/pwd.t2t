PWD

November 23, 2017

%!target: man
%!options(html): --mask-email

= NAME =

pwd -- shows the absolute path to current directory

= SYNOPSIS =

**pwd**

= DESCRIPTION =
  The **pwd** utility prints the full filename of the current working directory.
= EXIT STATUS =
The **pwd** returns 0 if it succeed. On error -1 is returned.
