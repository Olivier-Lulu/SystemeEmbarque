CP

November 24, 2017

%!target: man
%!options(html): --mask-email

= NAME =

cp -- copies given file to specified pathname if possible.

= SYNOPSIS =

**echo** SOURCE DEST

= DESCRIPTION =
The **cp** utility copies SOURCE to DEST if both are valid pathnames. It's a buffered copy. 

= EXIT STATUS =
The **cp** utility exists 0 if it could read the file, 1 if bad arguments are passed, or 2 if given pathnames are not valid. Upon error, errno is set appropriatly.
