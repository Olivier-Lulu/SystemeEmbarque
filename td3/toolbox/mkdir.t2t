MKDIR

November 24, 2017

%!target: man
%!options(html): --mask-email

= NAME =

mkdir -- creates a new and empty file at given pathname.

= SYNOPSIS =

**mkdir** DIRECTORY

= DESCRIPTION =
The **mkdir** utility creates an empty DIRECTORY if it is a valid pathname. Upon error, it sets errno appropriatly and returns -1.

= EXIT STATUS =
The **mkdir** utility exists 0 if it could create the file, or -1 if any occured.
