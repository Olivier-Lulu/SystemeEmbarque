LS

November 23, 2017

%!target: man
%!options(html): --mask-email

= NAME =

ls -- a function that is equivalent to ls -li in shell 

= SYNOPSIS =

**true**

= DESCRIPTION =
  The **ls** utility shows files in current directory.
= EXIT STATUS =
The **ls** utility returns 0 under normal circonstances or -1 if any error occurs.
