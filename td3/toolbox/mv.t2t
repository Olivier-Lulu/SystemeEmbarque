MV

November 24, 2017

%!target: man
%!options(html): --mask-email

= NAME =

mv -- renames a file

= SYNOPSIS =

**mv** SOURCE DEST

= DESCRIPTION =
The **mv** utility renames SOURCE to DEST if there are both valid pathnames. Tries first to rename SOURCE to DEST, but if it fails, mv will try to copy SOURCE to DEST and then unlink SOURCE.

= EXIT STATUS =
The **mv** utility exists 0 if it could rename the file, -1 if any error occured.
