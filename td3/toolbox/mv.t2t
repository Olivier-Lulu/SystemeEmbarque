MV

November 24, 2017

%!target: man
%!options(html): --mask-email

= NAME =

cat -- write file content to the standard output

= SYNOPSIS =

**cat** [filename]

= DESCRIPTION =
The **cat** utility writes a file content. If bad amount of argument is passed to cat, it will exits with an error, or set perror if the argument file is invalid.

= EXIT STATUS =
The **cat** utility exists 0 if it could read the file, 1 if bad arguments are passed, or 2 if file argument is invalid.
